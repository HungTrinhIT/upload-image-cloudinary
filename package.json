{
  "name": "upload-files-backend",
  "version": "1.0.0",
  "description": "Upload single and multiple files by using multer, express and cloudinary",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.js",
    "client": "npm run start --prefix client",
    "dev": "concurrently  \"npm run server\"  \"npm run client\" "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.3",
    "cloudinary": "^1.32.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "expressjs": "^1.0.1",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^2.0.20",
    "url": "^0.11.0"
  },
  "devDependencies": {
    "concurrently": "^7.5.0"
  }
}
